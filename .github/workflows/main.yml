name: Testing
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_test:
    name: test only
    runs-on: self-hosted
    timeout-minutes: 60
    steps:
      - name: Install and set Flutter version
        uses: subosito/flutter-action@v2
